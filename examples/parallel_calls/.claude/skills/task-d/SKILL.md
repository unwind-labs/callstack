# /task-d — Stock Report

Look up stock prices for AAPL and GOOGL.

## Procedure

1. Use `get_stock_price` for "AAPL"
2. Use `get_stock_price` for "GOOGL"

## Output

Return a JSON object:
```json
{"agent": "D", "AAPL": {stock}, "GOOGL": {stock}}
```
