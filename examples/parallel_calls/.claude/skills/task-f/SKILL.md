# /task-f — News Headlines

Look up news headlines for tech and finance.

## Procedure

1. Use `get_news_headline` for "tech"
2. Use `get_news_headline` for "finance"

## Output

Return a JSON object:
```json
{"agent": "F", "tech": {headline}, "finance": {headline}}
```
