# /task-b — Weather Report

Look up weather for Tokyo and London.

## Procedure

1. Use `get_weather` for "Tokyo"
2. Use `get_weather` for "London"

## Output

Return a JSON object:
```json
{"agent": "B", "tokyo": {weather}, "london": {weather}}
```
