# Run-tasks log: deepen-modules-overnight

Branch: refactor/deepen-modules-overnight
Start: 2026-05-25T19:34:26-07:00
Stashed: README.md as 'wip-readme-before-refactor'

## Tasks
1. Deepen report-assembly cluster (frames + reporter + invocation_ctx + finalize-glue)
2. Clean MCP boundary (mcp_server reaches into 5+ private names across 3 submodules)
3. Extract Invocation factory from Caller (~250 LOC of setup helpers)
4. Consolidate session-on-disk knowledge (3 modules know JSONL layout)

Mode: overnight (no user interaction, self-paced decisions)
Order: sequential — tasks 1/2/3 all touch __init__.py / mcp_server.py / frames.py / reporter.py

---

### Task 1 — start: 2026-05-25T19:35:19-07:00
Forking /call to deepen the report-assembly cluster.

### Task 1 — COMPLETE
Commit: facb304 "refactor: deepen report-assembly cluster behind InvocationReport facade"

**Design decisions**
- New `agent_callstack/report.py` — `InvocationReport` facade (213 LOC). A *deep*
  facade composing the existing hardened internals (frames/reporter/invocation_ctx),
  not a rewrite — the flock/fsync/PID-liveness/LRU-cache/content-hash machinery is
  battle-tested and was left intact behind the small interface.
- Interface: reporter(), seal() [absorbs the duplicated wait+finalize try/finally
  from Caller._invoke and Caller.resume], load_frames(), merged_document(),
  write_frame(), write_report(), finalize_own_frames(), + path/identity props.
  Plus `ROOT_FRAME_KEY` public alias and `InvocationReport.from_context()` escape
  hatch for callers that resolve nested-vs-root identity separately.
- Internals keep their underscore names so optimization tests can still poke them.

**Test migration policy (replace, don't layer)**
- Behavioral tests → assert through the facade.
- KEPT as labeled white-box (no honest boundary expression): PERF-* optimization
  tests (debounce coalescing, content-hash write-skip, parsed-frame LRU cache,
  dir-mtime glob-skip, dir-cache copy-independence) and the in-memory
  `_abandon_tree_nodes_in_place` primitive + shutdown-registry/signal tests.

**Files touched (LOC delta vs HEAD~1)**
 .../20260525-193426-deepen-modules.md              |  19 ++
 dev/run-tasks-logs/task1-plan.md                   |  78 ++++++++
 plugins/callstack/agent_callstack/__init__.py      |  33 ++--
 plugins/callstack/agent_callstack/report.py        | 213 +++++++++++++++++++++
 plugins/callstack/mcp_server.py                    |  18 +-
 tests/_helpers.py                                  |  32 ++--
 tests/test_mcp_server.py                           |  12 +-
 tests/test_orphan_reconciliation.py                | 175 ++++++++---------
 tests/test_report.py                               | 157 +++++++--------
 tests/test_shutdown_hardening.py                   | 103 +++++-----
 10 files changed, 563 insertions(+), 277 deletions(-)

**Test-count delta: 0** — 320 passing before and after (clean env).

IMPORTANT verification note: running `pytest` *inside a /call fork* shows 8
spurious failures (test_session mtime-fallback, test_api env-stamping, etc.)
because the forked session inherits CALLSTACK_* env vars that trip the
nested-invocation guards. Verified 320/320 with those env vars unset:
  env -u CALLSTACK_ROOT_INVOKE_ID -u CALLSTACK_ROOT_LOG_DIR -u CALLSTACK_DEPTH \
      -u CALLSTACK_FRAME_KEY -u CALLSTACK_OWN_SESSION -u CALLSTACK_MAX_DEPTH \
      -u CLAUDE_CODE_SESSION_ID -u CLAUDE_SESSION_ID python -m pytest tests/ -q
This affects how the orchestrator should verify Tasks 2-4 as well.

Reach-in audit (post-migration): test_orphan_reconciliation.py = 0 private
reach-ins; test_report.py + test_shutdown_hardening.py = reach-ins only inside
clearly-labeled white-box sections.

### Orchestrator decision
Deferring per-task thermo-nuclear-code-quality-review + codex:review + TODO remediation to a SINGLE pass at end of all 4 tasks. Reason: each task is large; running reviewers 4x over a growing branch is wasteful when one pass over the final diff finds the same issues plus cross-task interactions.

### Task 2 — start: 2026-05-25T19:54:51-07:00
Forking /call to clean the MCP boundary.

### Task 2 — COMPLETE
Commit: 298518d "refactor: clean MCP boundary — lift background-runs lifecycle into agent_callstack.background.BackgroundRuns"

Fork timed out before its final self-commit/log. Orchestrator inspected work,
verified 329 passing under clean env, and committed on its behalf with a
message reflecting the plan and diff.

**Design decisions** (from task2-plan.md)
- New agent_callstack/background.py with `BackgroundRuns` registry + six
  typed outcome dataclasses (Started, CapReached, Pending, Done, Crashed,
  NotFound). Caller validates → delegates → matches outcome → formats JSON.
- Public API additions: `new_invoke_id()`, `BackgroundRuns`, all outcomes,
  `max_fanout`, `max_background`, `root_identity` re-exported, `SessionLocator`
  added to `__all__`.
- mcp_server.py becomes a pure adapter: no private imports, no internal env
  module reach-in, no SessionLocator inline import. Background lifecycle
  reduces from inline dict + scheduling + reap + shield to thin delegations.

**Files touched**
 plugins/callstack/agent_callstack/__init__.py    |  34 ++++
 plugins/callstack/agent_callstack/background.py  | 224 ++++++ (new)
 plugins/callstack/mcp_server.py                  | 140 +++- (net -86)
 tests/test_background.py                         | 218 ++++++ (new)
 tests/test_mcp_server.py                         |  25 ++

**Test-count delta: +9** (320 → 329; new boundary tests for BackgroundRuns).


### Task 3 — COMPLETE
Plan: dev/run-tasks-logs/task3-plan.md

**Design decisions**
- New `agent_callstack/invocation.py` — `InvocationFactory`, a frozen,
  purely-config value object that owns the nested-vs-root identity decision
  and child-env propagation. Captures only a Caller's static config
  (explicit_cwd / log_dir / invoke_id / max_depth) and reads process env
  LAZILY at call time — so a host that pops stale CALLSTACK_ROOT_* between
  Caller construction and use (mcp_server does exactly this) still gets the
  corrected decision. Reads env via Task-2's public env helpers
  (root_identity / frame_key / claude_code_session / in_nested_invocation)
  rather than poking os.environ keys.
- Interface: parent_project_cwd(), effective_cwd(), effective_log_dir(),
  context(parent_cwd) [the nested-vs-root branch], child_env(ctx, depth_base).
- Caller keeps the behavioural seams `_parent_project_cwd`,
  `_resolve_invocation_context`, `_driver_for` but they become thin
  delegations to `self._inv`. `_effective_cwd` / `_effective_log_dir` removed
  (callers go through the factory). Channel/trace/store wiring stays in Caller
  — that's runtime config, not identity. Caller class shrank ~248 → 180 LOC;
  the ~95 LOC of identity helpers became ~25 LOC of delegation.
- mcp_server.py: NO change needed. It keeps its own `_invocation_identity`
  (validate inherited dir exists, pop stale env, mint for the envelope) — a
  boundary-adapter concern distinct from building a context. It already uses
  the public `root_identity()`; the factory uses the same primitive, so it's
  one rule with two consumers, not duplicated logic. Confirmed it still
  imports only top-level public `agent_callstack` names (Task 2 preserved).

**Files touched (LOC delta vs HEAD)**
 plugins/callstack/agent_callstack/__init__.py   | 114 (net -? ; Caller -68 LOC)
 plugins/callstack/agent_callstack/invocation.py | 140 (new)
 tests/test_invocation.py                        | 218 (new, 17 tests)

**Tests** — NEW tests/test_invocation.py: 17 direct boundary tests (root vs
nested context, frame_key fallback chain incl. pid fallback, distinct
instance_id per call, parent_project_cwd cross-project-fresh preference,
effective_log_dir default-vs-explicit, child_env stamping incl.
CALLSTACK_PARENT_SESSION omission + MAX_DEPTH propagation). Existing
test_invariant_child_parent.py + test_api.py left intact — they drive the
same factory through the preserved Caller seams (valuable end-to-end
invariants, not shallow-module duplication).

**Test-count delta: +17** (329 → 346). Verified 346 passing with the clean-env
recipe.

**Commit: a8aafcd** "refactor: extract InvocationFactory from Caller (Task 3)"
