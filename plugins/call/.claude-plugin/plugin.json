{
  "name": "call",
  "description": "Fork a sub-agent that inherits full conversation context, executes a task, and returns only the result — keeping the parent context clean.",
  "version": "0.1.0",
  "mcpServers": {
    "call": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"]
    }
  }
}
