[project]
name = "callstack"
version = "0.1.1"
description = "A call stack for AI agents to run deeply nested workflows reliably."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["plugins/callstack"]
